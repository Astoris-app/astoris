node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Astoris: lokale Daten (verschlüsselte Verbindungen, Master-Key) — NIEMALS committen
/data

# TLS-Zertifikate (privater Schlüssel!) — niemals committen
/certs

# Backups (können data/ oder .env enthalten) — niemals committen
/backups
